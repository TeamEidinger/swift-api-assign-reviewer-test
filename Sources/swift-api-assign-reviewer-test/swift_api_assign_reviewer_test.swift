public struct swift_api_assign_reviewer_test {
    public private(set) var text = "Hello, World!"

    public init() {
        // dummy change
    }

    public func test() {
        // requires reviewer
    }
}
